
{{#if this.icon}}
  <i class="calendar icon"></i>
{{/if}}
<Input @type="text" @value={{value}} placeholder={{this.placeholder}} @name={{name}} onkeydown="return false"
       @focus-out={{action "onChange"}}
   />
